/** Default Open Graph / X Card image (1200×630). Replace `public/og-site.png` to change branding site-wide. */
export const OG_SITE = {
  path: "/og-site.png",
  width: 1200,
  height: 630,
} as const;
